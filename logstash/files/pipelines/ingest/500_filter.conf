filter {
    mutate {
        remove_field => [ "headers", "@timestamp", "host", "@version" ]
    }
    kv {
        source => "message"
        field_split => " "
        value_split => "="
        prefix => "arg_"
        target => "kv"
    }
}